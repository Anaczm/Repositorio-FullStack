/* Operador ternário

const pontos = 109;

const nivelUsuario = pontos >= 100 ? 'Usuário vip' : 'Usuário normal'

console.log(nivelUsuario)*/

// laço for

const lista = ["mouse", "teclado", "lápis", "notebook"]

for(let i = 0; i < lista.length; i++){
    console.log(lista[i])
}

// exercício

